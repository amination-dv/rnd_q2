# =============================================================================
# CorrelationFields Inference Configuration
# =============================================================================

# Model architecture (must match training config)
backbone: mit_b0
in_channels: 1
img_height: 704
img_width: 480
num_tracks: 22
embedding_dim: 768

# Trained model weights
model_path: "../checkpoints/model.pt"
