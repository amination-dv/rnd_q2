# =============================================================================
# ILI Strip-ViT Inference Configuration
# =============================================================================

# Model architecture (must match training config)
backbone: dinov2_vits14
in_channels: 1
img_height: 308
img_width: 196
num_tracks: 22
num_classes: 4
patch_width: 14
intermediate_block_idx: 7

# Trained model weights
model_path: "../checkpoints/model.pt"

# Class metadata (order must match ImageFolder class_to_idx from training)
class_names: ["agm", "background", "stopple", "weld"]
background_class_idx: 1          # Index of 'background' in class_names
